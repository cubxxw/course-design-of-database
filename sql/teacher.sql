
"use teacher"
create table if not exists user (
    username varchar(20) primary key,
    passward varchar(20) not null );

insert into user values('teacher','22222222');

insert into user values('张三','22222222');

insert into user values('李四','22222222');

insert into user values('王五','22222222');
